spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/jobtracker}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:Adi@2004}

app.jwt.secret=${JWT_SECRET:JobTrackerSecretKeyForJWTTokenGenerationMustBeAtLeast256BitsLong!!}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}

spring.web.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173}
